---
layout: section
background: '/images/bg_inline_collections.jpg'
---

# 6. Inline collections

<!--
EasyAdmin's built-in CollectionField renders each entry as an expandable accordion panel. That works for long forms, but for compact tabular data — like a list of credited persons or setlist items — you want a table with one row per entry, inline fields, and drag-to-reorder. Let's build that.
-->
